package test ;
import iml.contracts.* ;
import iml.connectivity.* ;
import iml.software.* ;
import iml.lang.* ;
import iml.ports.* ;
type [ System ] S1 {
	[ Input ] i1 : DataPort < Real > ;
	[ Input ] i2 : DataPort < Real > ;
	[ Output ] o1 : DataPort < Real > ;
	[ Input ] n : DataPort < Int > ;
}
type [ System ] S2 {
	[ Input ] i1 : DataPort < Real > ;
	[ Input ] i2 : DataPort < Real > ;
	[ Output ] o1 : DataPort < Real > ;
	[ Input ] alpha : DataPort < Real > ;
}
type [ System ] S3 {
	[ Input ] i1 : DataPort < Real > ;
	[ Output ] o1 : DataPort < Real > ;
}
type [ Implementation ] S1_dot_Impl extends ( S1 ) {
	[ Component ] S2_sub : S2 ;
	[ Component ] S3_sub : S3 ;
	i1_TO_A : Connector < DataPort < Real > > ;
	assert { i1 = i1_TO_A . source && S2_sub . i1 = i1_TO_A . target } ;
}
