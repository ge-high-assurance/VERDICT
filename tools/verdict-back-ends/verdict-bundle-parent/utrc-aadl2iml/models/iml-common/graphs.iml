package iml.graphs;
import iml.lang.* ;
import iml.lang.utils.* ;



type Vertex ;

type Edge is (Vertex,Vertex);

type Graph {
	edges : List<Edge> ;
	add : Edge -> Graph := fun(x:Edge) {
		oneof Graph{edges = self.edges.push(x)}
	} ;
}



