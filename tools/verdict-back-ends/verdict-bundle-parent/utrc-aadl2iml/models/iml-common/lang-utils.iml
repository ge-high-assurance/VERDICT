package iml.lang.utils ;
import iml.lang.* ;


type List<T> {
	isEmpty : Bool ;
	head :T ;
	tail : List<T>;
	push : T -> List<T> := fun (x:T) {
    	oneof List<T> { head = x ; tail = self ; isEmpty = false }  
  	} ;
	
}